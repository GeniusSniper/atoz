import React from 'react';

class Cart extends React.Component{
    render(){
        let items = this.props.cart.map( item => {

        })
        
        return (
            null
        )
    }
}

export default Cart;